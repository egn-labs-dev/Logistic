services:
  vault:
    image: hashicorp/vault:latest
    cap_add: [IPC_LOCK]
    command: server -config=/vault/config/vault.hcl
    volumes:
      - ./config/vault.hcl:/vault/config/vault.hcl
      - vault-data:/vault/file
    environment:
      VAULT_ADDR: "http://0.0.0.0:8200"
      VAULT_API_ADDR: "http://0.0.0.0:8200"
    networks:
      - logistic-net

  vault-agent:
    image: hashicorp/vault:latest
    command: agent -config=/config/vault-agent.hcl
    volumes:
      - ./config:/config
      - shared-secrets:/vault/secrets
    depends_on:
      - vault
    networks:
      - logistic-net

  db:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_USER: log_admin
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: logistics_secure_db
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U log_admin -d logistics_secure_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - logistic-net

  pgbouncer:
    image: edoburu/pgbouncer:latest
    environment:
      DB_USER: log_admin
      DB_PASSWORD: ${DB_PASSWORD}
      DB_HOST: db
      DB_NAME: logistics_secure_db
      AUTH_TYPE: scram-sha-256
      POOL_MODE: transaction
      MAX_CLIENT_CONN: 1000
      DEFAULT_POOL_SIZE: 20
      LISTEN_PORT: 6432
      IGNORE_STARTUP_PARAMETERS: extra_float_digits, jit
    depends_on:
      db:
        condition: service_healthy
    networks:
      - logistic-net

  redis:
    image: redis:7-alpine
    restart: always
    networks:
      - logistic-net

  api:
    build: .
    restart: always
    depends_on:
      db:
        condition: service_healthy
      pgbouncer:
        condition: service_started
      redis:
        condition: service_started
      vault-agent:
        condition: service_started
      jaeger:
        condition: service_started
    volumes:
      - shared-secrets:/vault/secrets:ro
    env_file:
      - .env
    environment:
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4317
      - DATABASE_URL=postgresql+asyncpg://log_admin:${DB_PASSWORD}@pgbouncer:6432/logistics_secure_db
      - REDIS_URL=redis://redis:6379/0
    command: >
      sh -c "DATABASE_URL=postgresql+asyncpg://log_admin:$${DB_PASSWORD}@db:5432/logistics_secure_db alembic upgrade head && gunicorn app.main:app --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - logistic-net

  frontend:
    build: ./frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - logistic-net

  jaeger:
    image: jaegertracing/all-in-one:latest
    restart: always
    ports:
      - "16686:16686"
    networks:
      - logistic-net

networks:
  logistic-net:
    driver: bridge

volumes:
  pgdata:
  vault-data:
  shared-secrets:
    driver_opts:
      type: tmpfs
      device: tmpfs
